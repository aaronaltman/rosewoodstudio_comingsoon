/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "wp.rapidinsights.ai",
      "rosewoodstudio.wpenginepowered.com",
      "images.unsplash.com",
    ],
  },
};

export default nextConfig;
